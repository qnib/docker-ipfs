consul:
  image: qnib/consul
  ports:
   - "8500:8500"
  environment:
  - dc_name=dc1
  dns: 127.0.0.1
  hostname: consul
  privileged: true

ipfs:
  image: qnib/ipfs:0.3.7
  environment:
   - dc_name=dc1
  dns: 127.0.0.1
  volumes:
   - /data/
  ports:
   - 5001
   - 8080
  privileged: true
